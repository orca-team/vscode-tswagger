# Changesets

Changesets in this repository are used for the public npm packages only: `@tswagger/cli`, `@tswagger/core`, and `@tswagger/types`.

The VS Code extension uses Release Please for the root `package.json`, root `CHANGELOG.md`, and `extension-vX.Y.Z` tags. Do not add extension-only release notes here.
