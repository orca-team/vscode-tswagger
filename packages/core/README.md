# @tswagger/core

Low-level generation primitives from the `tswagger` monorepo for integrations and secondary tooling.

Most users should start with `@tswagger/cli` or the VS Code extension instead of depending on this package directly.

Release notes are tracked in `packages/core/CHANGELOG.md`.