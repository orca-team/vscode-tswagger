{
  "name": "@tswagger/types",
  "version": "1.0.0-beta.0",
  "private": false,
  "description": "Shared public TypeScript types for the tswagger ecosystem.",
  "license": "MIT",
  "repository": "https://github.com/orca-team/vscode-tswagger.git",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepack": "pnpm run build",
    "pack:check": "npm pack --dry-run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openapi-types": "^12.1.0"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
