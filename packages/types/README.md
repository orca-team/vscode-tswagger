# @tswagger/types

Shared public TypeScript types for the `tswagger` ecosystem.

This package is mainly intended for integrators and tooling authors. Most users should start with `@tswagger/cli` or the VS Code extension instead.

Release notes are tracked in `packages/types/CHANGELOG.md`.