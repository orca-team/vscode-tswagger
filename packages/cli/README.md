# @tswagger/cli

CLI for generating TypeScript artifacts from Swagger/OpenAPI v2 documents.

This package depends on the published `@tswagger/core` and `@tswagger/types` packages. Most users should start here instead of consuming the lower-level packages directly.

Release notes are tracked in `packages/cli/CHANGELOG.md`.